# Minimal FLPR config: IPC service + ICMsg backend, no console, no shell.
CONFIG_IPC_SERVICE=y
CONFIG_IPC_SERVICE_BACKEND_ICMSG=y
CONFIG_MBOX=y

# GRTC for k_cycle_get_32() hardware epoch (non-zero, monotonic).
CONFIG_NRFX_GRTC=y

# Logging (minimal, no UART backend — only IPC).
CONFIG_LOG=y
CONFIG_LOG_MODE_MINIMAL=y

# Small heap for IPC service internal allocations
CONFIG_HEAP_MEM_POOL_SIZE=1024

# Ring processing needs stack buffer; increase main thread stack
CONFIG_MAIN_STACK_SIZE=4096

# Disable unused subsystems
CONFIG_SHELL=n
CONFIG_SERIAL=n
CONFIG_UART_INTERRUPT_DRIVEN=n
CONFIG_CONSOLE=n

# R8: FLPR-image acceptance diagnostics (RING_TEST/STALL/STRESS/
# FAULT_HANG handlers + diagnostic counters).  Enabled for the current
# nRF54L15 acceptance build; the release default is off (separate work).
# Cpuapp-side acceptance is gated by CONFIG_AUDIO_ACCEPTANCE_DIAGNOSTICS.
CONFIG_FLPR_ACCEPTANCE_DIAGNOSTICS=y
