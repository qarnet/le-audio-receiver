# v0.0.1 pre-refactor testing baseline

Version: T0, 2026-07-31.

## Tag anchor

- Release tag `v0.0.1` points to commit
  `c608e460627ce7f9858c9dcea890848aa3a6a28f` (Merge pull request #2:
  feat/nrf54l15-asrc-flpr).
- The v0.0.1 tag shipped Phase 5 (cpuapp fixed-point linear ASRC) and
  Phase 6 Stages 0–5 (FLPR offload).  FLPR ASRC offload was accepted;
  at Phase 6 close (pre-tag), 432 unit tests (396 C + 36 Python) passed.
  This count does not include the subsequent BlueZ/WirePlumber Python suites.
  nRF54L15 hardware: Mode A + Mode B 120 s at 100 fps, zero faults.

## RF-switch fix (post-tag)

- PR #3 (`fix/xiao-rf-switch`) adds Xiao RF-switch control and 16 pF crystal
  configuration on top of `v0.0.1`.  This fix was **not** in the `v0.0.1` tag.
- PR #3 was merged as `20b37c405835e5c2c747fa7b072c4c0b752b29cd` into
  `origin/main`.
- Commit history on the merged branch: `boards: nrf54l15: add Xiao RF switch
  and crystal overrides` (f0a23cd) + `docs: correct RF-switch handoff results
  with review retest evidence` (05a78b3).

## Manual connection confirmation (post-PR-#3)

- User manually confirmed normal BAP connection after the RF correction was
  applied.  Streaming was not independently confirmed at this point.
- Evidence provenance: current conversation/session and existing
  `docs/development/xiao-rf-switch-fix-handoff.md`.
- Manual connection is useful acceptance evidence but is not automated
  regression protection.

## Pre-merge PR #3 verification

- Full gate 21/21 passed on the provisioned workstation (`thomas-workstation`).
- `fw-build-5340` passed.
- `fw-build-54l15` passed.
- Verification was run by the handoff executor during PR review, not as an
  automated CI step.

## Evidence provenance

All counters and evidence in this document come from:

- The current conversation/session (2026-07-31).
- `docs/development/xiao-rf-switch-fix-handoff.md` (merged with PR #3).
- `docs/development/phase5-hardware-acceptance-results.md` (Phase 5 acceptance).
- `docs/development/phase6-stage0-results.md` through
  `docs/development/phase6-stage5-results.md` (Phase 6 acceptance).

No counters or evidence are invented.  Where evidence is absent, this document
says so rather than inferring.

## Relationship to testing plan

- `v0.0.1` tag is the release/history anchor.
- Merged `origin/main` after PR #3 is the implementation base for the
  pre-refactor testing plan defined in
  `docs/development/pre-refactor-testing-plan.md`.
- This baseline document records what was known and verified before T0
  started.  T0 does not change any production behavior.
