CONFIG_ZTEST=y

# The comparator suite has no entropy consumer. Disable native_sim's fake
# entropy driver and its otherwise-empty parent library so the test run emits
# no native entropy warning.
CONFIG_FAKE_ENTROPY_NATIVE_SIM=n
CONFIG_ENTROPY_GENERATOR=n
