# Minimal GPIO pattern test for logic-analyzer probe verification.
# No BLE, no I2S, no audio — just GPIO + console for boot banner.

CONFIG_GPIO=y
CONFIG_CONSOLE=y
CONFIG_SERIAL=y
CONFIG_UART_CONSOLE=y

# Keep image small
CONFIG_PRINTK=y
CONFIG_LOG=n
CONFIG_BT=n
